# =============================================================================
# Prophit Frontend — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in your values.

# --- Public (exposed to browser via NEXT_PUBLIC_ prefix) ---
NEXT_PUBLIC_VAULT_ADDRESS=0x...               # ProphitVault contract address
NEXT_PUBLIC_ADAPTER_A_ADDRESS=0x...           # Adapter A contract address
NEXT_PUBLIC_ADAPTER_B_ADDRESS=0x...           # Adapter B contract address
NEXT_PUBLIC_USDT_ADDRESS=0x55d398326f99059fF775485246999027B3197955  # BSC USDT
NEXT_PUBLIC_RPC_URL=http://127.0.0.1:8545     # RPC URL for wagmi provider
NEXT_PUBLIC_CHAIN_ID=31337                    # 56 for BSC mainnet, 31337 for Anvil
NEXT_PUBLIC_CHAIN_NAME=Anvil                  # Display name for the chain

# --- Server-side only (not exposed to browser) ---
AGENT_URL=http://localhost:3001               # Agent HTTP API URL
AGENT_API_KEY=                                # Must match agent's API_KEY
